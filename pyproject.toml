[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "geckopy"
version = "0.2.1"
description = "Enzyme-constrained genome-scale metabolic modeling in Python, inspired by the GECKO Toolbox"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [
    { name = "Eduard Kerkhoven", email = "eduardk@chalmers.se" },
]
keywords = [
    "ecmodel",
    "enzyme-constrained",
    "gecko",
    "metabolic-model",
    "cobra",
    "systems-biology",
    "constraint-based-modeling",
]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Bio-Informatics",
]
dependencies = [
    "cobra>=0.29",
    "numpy>=1.21",
    "pandas>=2",
    "pydantic>=2",
    # raven-toolbox provides the underlying model-manipulation primitives geckopy
    # extends, plus the ecModel YAML I/O (raven_toolbox.io.ec_data.EcData and the
    # ec-rxns / ec-enzymes / gecko_light sections in read/write_yaml_model). Not
    # yet on PyPI; pinned to the SysBioChalmers develop branch.
    "raven-toolbox @ git+https://github.com/SysBioChalmers/raven-toolbox.git@develop",
    "requests>=2.28",
    "ruamel.yaml>=0.17",
    "scipy>=1.10",
    "tqdm>=4.65",
]

[project.optional-dependencies]
dev = [
    "pytest>=7",
    "pytest-cov",
    "ruff>=0.4",
]
tutorial = [
    "matplotlib>=3.5",
]

[project.urls]
Homepage = "https://github.com/SysBioChalmers/geckopy"
Source = "https://github.com/SysBioChalmers/geckopy"
Issues = "https://github.com/SysBioChalmers/geckopy/issues"
"GECKO MATLAB" = "https://github.com/SysBioChalmers/GECKO"
"raven-toolbox" = "https://github.com/SysBioChalmers/raven-toolbox"

[project.scripts]
geckopy = "geckopy.cli:main"

[tool.hatch.metadata]
# Allow `raven-toolbox @ git+https://...` (PEP 508 direct URL) in
# project.dependencies. hatchling refuses direct references by default
# because PyPI rejects sdists that contain them — geckopy can't go on
# PyPI yet anyway (name is taken), and raven-toolbox isn't on PyPI yet
# either, so the git URL is the only option for the alpha series.
# Re-evaluate when raven-toolbox ships to PyPI.
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/geckopy"]

[tool.pytest.ini_options]
testpaths = ["tests"]
# Smoke tests are opt-in (they need external data and can take minutes);
# run them with `pytest -m smoke`. The default invocation excludes them.
addopts = "-m 'not smoke'"
markers = [
    "smoke: real-scale smoke tests that need external data (e.g. Human-GEM). Opt-in with `pytest -m smoke`; auto-skipped when the data isn't present.",
]

[tool.ruff]
line-length = 100
target-version = "py311"
extend-exclude = ["tutorials/"]

[tool.ruff.lint]
# Start with actual-error rules only; modernization (I, UP, B) and
# import sorting can be enabled in a follow-up PR after a one-shot
# `ruff check --fix && ruff format` cleanup pass.
select = ["E", "F", "W"]
ignore = [
    "E501",  # line length handled separately by formatter, not lint
    "E702",  # `R.lower_bound = 0; R.upper_bound = 1000` is intentional in test fixtures
]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["E402"]  # allow late imports in test files
